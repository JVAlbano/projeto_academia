package br.com.redeacademia.model.enums;

/**
 * Estados possiveis de uma {@code Matricula} (maquina de estados).
 */
public enum StatusMatricula {
    ATIVA,
    SUSPENSA,
    CANCELADA,
    VENCIDA
}
